"use client";

import { ICustomFilterProps } from "@/utils/interface";
import React from "react";

const CustomFilter: React.FC<ICustomFilterProps> = ({ title }) => {
  return <div>custom filter</div>;
};

export default CustomFilter;
